## Version Notes

### v0.1 Initial Model
This version implements the first working Math Tutor RL environment and tabular Q-learning model.  
The initial comparison showed that Q-learning did not outperform the simple weakest-skill baseline, which motivated further refinement of the reward function and environment design.
